# IA Analyzer — dependencias (GUI + pipeline + descarga de modelo)
# Uso: pip install -r requirements.txt
# La LLM (llama-cpp-python) se instala con el script de entorno; en Windows usa wheel CPU.

pandas>=1.5.0
requests>=2.28.0
scikit-learn>=1.2.0
huggingface_hub>=0.20.0
